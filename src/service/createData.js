export default function createData(id, ddSemana, data, lembrete, notas, treino, leuHoje, tirouFoto, foto, dieta, lendo) {
    return {
        id,
        ddSemana,
        data,
        lembrete,
        notas,
        treino,
        leuHoje,
        tirouFoto,
        foto,
        dieta,
        lendo
    };
}
